# Finding: IDOR at line 151
# Severity: Medium
# Evidence: user_id = request.args.get('user_id')
# File: unknown

Analyze this security finding:

Finding Type: IDOR
Severity: Medium
Evidence: user_id = request.args.get('user_id')
Line: 151

Code context:
```
142:     # However, still weak crypto
143:     c.execute(f"UPDATE users SET password = '{hashed}' WHERE id = {session['user_id']}")
144:     conn.commit()
145:     conn.close()
146:     return "Password updated (insecure hash)"
147: 
148: # Profile page: shows user info, vulnerable to IDOR (Insecure Direct Object Reference)
149: @app.route('/profile')
150: def profile():
151:     user_id = request.args.get('user_id')
152:     if not user_id and 'user_id' in session:
153:         user_id = session['user_id']
154:     if not user_id:
155:         return "Please login", 401
156:     # Directly using user_id without checking ownership
157:     conn = get_db()
158:     c = conn.cursor()
159:     c.execute(f"SELECT username, role FROM users WHERE id = {user_id}")
160:     user = c.fetchone()
161:     conn.close()
```

Provide:
1. Explanation of the vulnerability
2. How to fix it (show code example)



# RESPONSE FORMAT:
# Provide your analysis in the following format:
# ---EXPLANATION---
# [Your explanation here]
# ---FIX_SUGGESTION---
# [Your fix suggestion with code example here]
