# Finding: SQL Injection at line 23
# Severity: Critical
# Evidence: c = conn.cursor()
# File: unknown

Analyze this security finding:

Finding Type: SQL Injection
Severity: Critical
Evidence: c = conn.cursor()
Line: 23

Code context:
```
14: app.secret_key = 'hardcoded_secret_key_12345'  # Hardcoded secret key (vulnerability)
15: 
16: # Hardcoded admin credentials
17: ADMIN_USERNAME = "admin"
18: ADMIN_PASSWORD_HASH = hashlib.md5(b"SuperSecretPass123").hexdigest()  # MD5 & hardcoded
19: 
20: # Initialize database (vulnerable to SQL injection)
21: def init_db():
22:     conn = sqlite3.connect('vuln_app.db')
23:     c = conn.cursor()
24:     c.execute('''CREATE TABLE IF NOT EXISTS users
25:                  (id INTEGER PRIMARY KEY, username TEXT, password TEXT, role TEXT)''')
26:     c.execute("INSERT OR IGNORE INTO users VALUES (1, 'admin', ?, 'admin')",
27:               (hashlib.md5(b"admin123").hexdigest(),))
28:     c.execute("INSERT OR IGNORE INTO users VALUES (2, 'testuser', ?, 'user')",
29:               (hashlib.md5(b"pass123").hexdigest(),))
30:     conn.commit()
31:     conn.close()
32: 
33: init_db()
```

Provide:
1. Explanation of the vulnerability
2. How to fix it (show code example)



# RESPONSE FORMAT:
# Provide your analysis in the following format:
# ---EXPLANATION---
# [Your explanation here]
# ---FIX_SUGGESTION---
# [Your fix suggestion with code example here]
