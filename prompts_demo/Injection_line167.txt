# Finding: Injection at line 167
# Severity: Critical
# Evidence: # Command injection via eval (dangerous)
# File: unknown

Analyze this security finding:

Finding Type: Injection
Severity: Critical
Evidence: # Command injection via eval (dangerous)
Line: 167

Code context:
```
158:     c = conn.cursor()
159:     c.execute(f"SELECT username, role FROM users WHERE id = {user_id}")
160:     user = c.fetchone()
161:     conn.close()
162:     if user:
163:         return f"Profile: {user[0]} (Role: {user[1]})<br><a href='/'>Home</a>"
164:     else:
165:         return "User not found"
166: 
167: # Command injection via eval (dangerous)
168: @app.route('/calculator')
169: def calculator():
170:     expr = request.args.get('expr', '')
171:     # Evaluate arbitrary Python expressions
172:     try:
173:         result = eval(expr)
174:         return f"Result: {result}"
175:     except Exception as e:
176:         return f"Error: {e}"
177: 
```

Provide:
1. Explanation of the vulnerability
2. How to fix it (show code example)



# RESPONSE FORMAT:
# Provide your analysis in the following format:
# ---EXPLANATION---
# [Your explanation here]
# ---FIX_SUGGESTION---
# [Your fix suggestion with code example here]
