# Finding: SQL Injection at line 67
# Severity: Critical
# Evidence: c = conn.cursor()
# File: unknown

Analyze this security finding:

Finding Type: SQL Injection
Severity: Critical
Evidence: c = conn.cursor()
Line: 67

Code context:
```
58: 
59: # SQL Injection vulnerability
60: @app.route('/login', methods=['POST'])
61: def login():
62:     username = request.form.get('username')
63:     password = request.form.get('password')
64:     # Construct query with direct concatenation
65:     query = f"SELECT * FROM users WHERE username = '{username}' AND password = '{hashlib.md5(password.encode()).hexdigest()}'"
66:     conn = get_db()
67:     c = conn.cursor()
68:     try:
69:         c.execute(query)
70:         user = c.fetchone()
71:         if user:
72:             session['user_id'] = user[0]
73:             session['role'] = user[3]
74:             return redirect(url_for('profile'))
75:         else:
76:             return "Login failed", 401
77:     except Exception as e:
```

Provide:
1. Explanation of the vulnerability
2. How to fix it (show code example)



# RESPONSE FORMAT:
# Provide your analysis in the following format:
# ---EXPLANATION---
# [Your explanation here]
# ---FIX_SUGGESTION---
# [Your fix suggestion with code example here]
